{
  "routes": [
    {
      "method": "GET",
      "path": "/circles",
      "handler": "Circle.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/circles/count",
      "handler": "Circle.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/circles/:id",
      "handler": "Circle.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/circles",
      "handler": "Circle.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/circles/:id",
      "handler": "Circle.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/circles/:id",
      "handler": "Circle.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
